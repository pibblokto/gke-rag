provider:
  name: cloudflare

sources:
  - traefik-proxy

domainFilters:
  - mukinabaht.com

registry: txt
txtOwnerId: traefik
policy: upsert-only

env:
- name: CF_API_TOKEN
  valueFrom:
    secretKeyRef:
      name: cloudflare-api-key
      key: apiToken
